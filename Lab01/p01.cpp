#include <iostream>

struct Node{
  int data;
  Node* prev;
  Node* next;

  Node(int aData, Node* aNext, Node* aPrev)
  : data(aData), next(aNext), prev(aPrev)
  {
  }


}

int main(){
  int z;
  while (cin >> z) {
    new Node(z, nullpointer,);
  }
  return 0;
}
